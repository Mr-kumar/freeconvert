/**
 * API Route: POST /api/compress
 * Logic: Proxies image compress request to backend. Body: { fileKeys, options? }. Returns { jobId }.
 */

export async function POST() {
  return new Response();
}
