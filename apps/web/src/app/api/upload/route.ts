/**
 * API Route: POST /api/upload
 * Logic: Proxies file upload to backend. Accepts multipart/form-data, forwards to backend
 * upload endpoint, returns { fileKey } or error.
 */

export async function POST() {
  return new Response();
}
